const createToDo = (title, description, dueDate, priority, project) => {
  return { title, description, dueDate, priority, project };
};

//add todo item to project

//remove todo item from project

//delete todo item

export { createToDo };
